const verifyApiKey = (key) => {
  return;
};

const sendToTelemetry = (data) => {
  //console.log("Telemetry data =", data);
  return;
};

const sendConnect = (data) => {
  //console.log("Connect data =", data);
  return;
};

const sendDisconnect = (data) => {
  //console.log("Disconnect data =", data);
  return;
};

module.exports = {
  verifyApiKey,
  sendToTelemetry,
  sendConnect,
  sendDisconnect,
};
